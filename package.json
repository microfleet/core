{
  "name": "microfleet",
  "version": "18.7.0",
  "private": true,
  "scripts": {
    "semantic-release": "semantic-release",
    "prepare": "mdep install",
    "prebuild": "pnpm clean",
    "clean": "pnpm -r clean",
    "build": "pnpm tsc -b ./tsconfig.build.json",
    "pretest": "pnpm build",
    "test": "pnpm test -r --workspace-concurrency 1",
    "version": "rimraf ./ci/staged/*.json; pnpm --filter=./packages exec -- pnpm release-it"
  },
  "devDependencies": {
    "@makeomatic/deploy": "^12.6.3",
    "@node-rs/xxhash-linux-x64-musl": "^1.2.0",
    "@release-it/conventional-changelog": "^4.2.2",
    "@swc/core-linux-x64-musl": "^1.2.160",
    "conventional-changelog-angular": "^5.0.13",
    "glob": "^7.2.0",
    "js-yaml": "^4.1.0",
    "prepend-file": "^2.0.1",
    "release-it": "^14.13.1",
    "rimraf": "^3.0.2",
    "typescript": "~4.6.3"
  }
}
