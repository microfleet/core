{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "prebootstrap": "yarn --frozen-lockfile --check-files",
    "bootstrap": "lerna bootstrap -- --frozen-lockfile --check-files",
    "lint": "lerna run lint --stream",
    "pretest": "yarn lint",
    "test": "lerna run test --stream",
    "posttest": "yarn compile",
    "precompile": "yarn clean",
    "compile": "tsc -b packages/*/tsconfig.build.json",
    "clean": "rimraf packages/*/lib",
    "semantic-release": "lerna version --conventional-commits --github-release --yes"
  },
  "devDependencies": {
    "@makeomatic/deploy": "^8.4.4",
    "lerna": "^3.13.2",
    "rimraf": "^2.6.2",
    "tslint": "^5.15.0",
    "tslint-config-blvd": "^1.2.1",
    "typescript": "^3.4.2"
  },
  "husky": {
    "hooks": {
      "commit-msg": "commitlint -e $HUSKY_GIT_PARAMS",
      "prepare-commit-msg": "./node_modules/@makeomatic/deploy/git-hooks/prepare-commit-msg $HUSKY_GIT_PARAMS"
    }
  }
}
