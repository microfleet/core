{
  "$id": "microfleet.core",
  "type": "object",
  "additionalProperties": true,
  "required": ["name", "plugins"],
  "properties": {
    "name": {
      "type": "string",
      "pattern": "^[a-zA-Z0-9_\\-]{3,32}$",
      "description": "service name"
    },
    "maintenanceMode": {
      "type": "boolean",
      "default": false,
      "description": "flag to display 418 status message for actions not marked as readonly"
    },
    "plugins": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "list of plugins which will be loaded on service start"
    },
    "addPlugins": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "additional plugins which will be added to existing list"
    },
    "connectTimeoutMs": {
      "type": "integer",
      "exclusiveMinimum": 0,
      "description": "service will fall if it's not connected in reasonable amount of time",
      "default": 60000
    }
  }
}
